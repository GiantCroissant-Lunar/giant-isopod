{
  "providers": [
    {
      "id": "pi",
      "displayName": "Pi",
      "executable": "pi",
      "args": ["--mode", "text", "--no-session", "--provider", "{provider}", "--model", "{model}", "-p", "{prompt}"],
      "env": {
        "FORCE_COLOR": "1",
        "ZAI_API_KEY": "{ZAI_API_KEY}"
      },
      "defaults": {
        "provider": "zai",
        "model": "glm-4.7"
      }
    },
    {
      "id": "kilo",
      "displayName": "Kilo",
      "executable": "kilo",
      "args": ["run", "{prompt}"],
      "env": {},
      "defaults": {}
    },
    {
      "id": "codex",
      "displayName": "Codex",
      "executable": "codex",
      "args": ["exec", "{prompt}", "--full-auto"],
      "env": {},
      "defaults": {}
    },
    {
      "id": "copilot",
      "displayName": "GitHub Copilot CLI",
      "executable": "copilot",
      "args": ["-p", "{prompt}", "--silent", "--stream", "off", "--yolo", "--no-ask-user", "--no-custom-instructions", "--model", "{model}"],
      "env": {
        "FORCE_COLOR": "1"
      },
      "defaults": {
        "model": "gpt-5.2-codex"
      }
    },
    {
      "id": "gemini",
      "displayName": "Gemini CLI",
      "executable": "gemini",
      "args": ["-m", "{model}", "-p", "{prompt}", "-y", "-o", "text"],
      "env": {},
      "defaults": {
        "model": "gemini-2.5-flash"
      }
    },
    {
      "id": "claude-code",
      "displayName": "Claude Code",
      "executable": "claude",
      "args": ["-p", "--output-format", "text", "--dangerously-skip-permissions", "--tools", "Bash,Edit,Read,Write", "--append-system-prompt", "Follow the assigned task literally. Exact filenames, paths, content, and do-not-modify constraints are hard requirements. If the task names one target file, do not modify any other file. If you cannot satisfy the exact task, explain that in failure_reason instead of improvising.", "--model", "{model}", "--no-session-persistence", "{prompt}"],
      "env": {},
      "defaults": {
        "model": "sonnet"
      }
    },
    {
      "id": "kimi",
      "displayName": "Kimi",
      "executable": "kimi",
      "args": ["--print", "-p", "{prompt}"],
      "env": {},
      "defaults": {}
    }
  ]
}
