# Ignore everything (installed external skills)
*

# Except these files
!.gitignore
!skills.json
!install_skills.py

# Track agent-browser skill folder
!agent-browser/

# Track memory skill (local, not installed)
!memory/
!memory/**
